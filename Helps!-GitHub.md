>>> G I T H U B 
>>> C O M M A N D
run with CMD ...

========================
# ดึงโค้ดจาก Git
git pull

# ดึงเเบบ "HARD PULL"
git fetch --all
git reset --hard origin/main
git clean -fd

========================
# อัพโค้ดขึ้น Git
git add .
git status
git commit -m "code commit"
git push